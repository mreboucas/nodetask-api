let express,consign;_596‍.w("express",[["default",["express"],function(v){express=v}]]);_596‍.w("consign",[["default",["consign"],function(v){consign=v}]]);



const PORT = 3000;

const app = express();

//Format JSON response
/**
 * Não vi diferença com essa versão do NODE com express, dessa formatação. Mas adiante...
 */
app.set("json spaces", 10);

app.set("port", 3000)

consign()
.include("model2")
.then("libs/middlewares.js")
.then("routes")
.then("libs/boot.js")
.into(app);

//app.get("/", (req, res) => res.json({status: "NTask API"}));

/**
 * 
 app.get("/tasks", (req, res) => {
     res.json({
         tasks: [
             {title:"Fazer compras"},
             {title:"Consertar o pc"},
            ]
        });
});
*/

//app.listen(PORT, () => console.log(`Node task API - porta ${PORT}`));